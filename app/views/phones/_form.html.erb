<%= form_for(phone) do |f| %>
  <% if phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="field">
    <%= f.label :whatsapp %>
    <%= f.text_field :whatsapp %>
  </div>

  <div class="field">
    <%= f.label :inscription_id %>
    <%= f.text_field :inscription_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
